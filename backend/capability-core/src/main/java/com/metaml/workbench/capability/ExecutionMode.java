package com.metaml.workbench.capability;

// Whether a capability provider is expected to complete inline or via a separate completion signal.
public enum ExecutionMode {
    SYNCHRONOUS,
    ASYNCHRONOUS
}
